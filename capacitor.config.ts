import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eslam.personalaccountant',
  appName: 'personal-accountant',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
